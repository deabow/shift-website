import { PageShell } from "@/components/page-shell";

export default function SecurityPage() {
  return (
    <PageShell
      eyebrow="Security"
      title="Security-first delivery as a core agency standard."
      description="SHIFT builds with secure defaults, clean dependency hygiene, and architecture that protects brand, users, and business continuity from day one."
    />
  );
}
